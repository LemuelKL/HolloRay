x = 3;
y = 5;
z = 7;
t = 1;

w = -t * (x - (y + z)) * (x + (y - z)) * (x + (y + z)) * (x - (y - z));

w = w / (y * y);

println("The value of w is:", w);

b = 4;
exp = 3;
res = b**exp;

print("The value of res is:");
print(res);
